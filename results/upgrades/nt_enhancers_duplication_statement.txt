Exact full-sequence string matching (after the benchmark's own `strip().upper()` normalization, on the pinned Nucleotide Transformer dataset revision 96d86d567d...) shows that 100 of the 400 nt_enhancers TEST sequences (25.0%) are VERBATIM duplicates of a TRAINING sequence (train->test leakage), and 0 test sequences (0.0%) are exact duplicates of at least one OTHER test sequence (within-test redundancy); 0 sequences fall in both categories. Of the 400 test sequences, 400 are distinct strings and 300 (75.0%) are clean (neither train-duplicated nor test-duplicated). The train->test verbatim count matches the F003 8-mer Jaccard=1.0 count exactly (100), confirming the flagged near-duplicates are true verbatim sequence duplications, not an artifact of the k-mer presence metric.
